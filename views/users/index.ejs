<link rel="stylesheet" href="/public/css/user/userInfo.css" />
<link rel="stylesheet" href="/public/css/group/group.css" />

<main class="participants contenedor">
	<h1><%= title %></h1>
	<div class="userInfo">
		<div class="image">
			<% if(userProfile.pfp) { %>
				<img
					src="/public/user/<%- userProfile.id %>/<%- userProfile.pfp %>"
					alt="<%- userProfile.name %> profile picture"
				/>
			<% } %>
		</div>
		
		<div class="text">
			<%- userProfile.description %>
		</div>
	</div>
	
	<div>
		<% if(groups.length > 0) { %>
			<h2>Groups of this user</h2>
			<ul>
				<% groups.forEach((group) => { %>
					<li class="group">
						<div class="groupImage">
							<% if(group.image) { %>
								<img
									src="/public/uploads/groups/<%- group.image %>"
									alt="<%- group.name %> image"
									width="360"
									height="180"
								/>
							<% } else { %>
								<img
									src="/public/img/meeti.png"
									alt="Meeti logo"
									width="360"
									height="180"
								/>
							<% } %>
						</div>
						
						<div class="text">
							<a href="/groups/<%- group.id %>">
								<%- group.name %>
							</a>
						</div>
					</li>
				<% }); %>
			</ul>
		<% } %>
	</div>
</main>
